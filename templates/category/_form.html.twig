{{ form_start(form) }}

    <form data-bitwarden-watching="1">
        <div class="form-group">
        <label class="form-label mt-4">Catégorie</label>
        <input type="text" class="form-control" placeholder="Entrer la catégorie" name="{{ field_name(form.name) }}" value="{{ field_value(form.name) }}">
        </div>

        <button class="btn btn-primary my-4">{{ button_label|default('Ajouter') }}</button>
    {{ form_widget(form) }}
    </form>
{{ form_end(form) }}
